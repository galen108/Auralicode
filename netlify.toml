[build]
command = "npm run build"
publish = "dist/auralicode/browser"

[[redirects]]
from = "/*"
to = "/.netlify/functions/server"
status = 200

# Tell Netlify to use the node-internal build for SSR
[functions]
node_bundler = "esbuild"
